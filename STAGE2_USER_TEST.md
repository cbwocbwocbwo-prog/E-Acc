# 2단계 영수증 OCR 사용자 테스트 안내

현재 2단계는 원본 영수증 다운로드와 기본 OCR 비교 기능을 개발 시험 중입니다.

## 현재 구현된 동작

1. 최신 조회 결과의 `증빙유무`를 기준으로 상태를 구분한다. `#`만 OCR 검증 대상이다.
2. `선택 행 영수증 검증`을 누른다.
3. 프로그램이 e-Accounting 웹 화면에서 같은 행을 정확히 찾는다.
4. 결재선 지정 창의 코드에서 원본 영수증 주소를 추출한다.
5. 원본 이미지를 로컬 PC에 다운로드한다.
6. Windows 한국어 OCR로 승인번호·증빙일자·사용금액을 읽는다.
7. 필요하면 원본을 변경하지 않고 0°·90°·180°·270° 방향으로 OCR해 가장 정확한 방향을 선택한다.
8. 화면값과 OCR값을 비교해 `정상` 또는 `이상`과 필드별 사유를 표시한다.
9. `#` 행인데 결재선 창에 실제 영수증 이미지가 없으면 해당 행을 `재조회 필요`로 표시하고,
   최신 미상신내역을 다시 불러와 `Y/#/N` 상태 변화를 확인한다.
10. `# 전체 영수증 OCR 검증`을 누르면 현재 조회된 모든 `#` 행을 순차 다운로드·판독한다.
11. 영수증 결재선 팝업은 열린 직후 숨김 처리한다. 로그인과 목록 조회에 쓰는 Edge 창은 기존처럼 표시한다.

## 증빙유무의 최신 상태 처리

| 상태 | 의미 | 영수증판정 |
|---|---|---|
| `#` | 증빙 동기화 완료 | OCR 검증 진행 |
| `Y` | 사용자가 증빙 등록, 시스템 동기화 대기 | 동기화 대기 |
| `N` | 사용자 증빙 미등록 | 영수증 미등록 |

증빙유무는 동기화에 따라 바뀌므로, 미상신내역을 다시 불러오면 이전 영수증 판정은 초기화하고 최신 상태를 기준으로 다시 판정한다.

## 사용자 시험 순서

1. 현재 열린 첫 영수증의 세 항목과 이미지가 올바른 행에 연결됐는지 확인한다.
2. `#`인 다른 행을 여러 개 선택해 영수증 형태가 달라도 정확히 연결되는지 확인한다.
3. 흐리거나 기울어진 영수증, 숫자가 여러 번 표시된 영수증을 우선 시험한다.
4. 세로 또는 거꾸로 저장된 영수증의 결과 창에 적용 회전각도가 표시되는지 확인한다.
5. `#` 행 중 실제 이미지가 없는 사례는 `재조회 필요` 및 영수증사유로 남는지 확인한다.
6. `N` 또는 `Y` 행을 선택했을 때 `영수증 없음`으로 분리되는지 확인한다.
7. 불일치 사례에서 `영수증사유`에 어떤 필드가 다른지 표시되는지 확인한다.

## 현재 첫 표본 결과

- 승인번호: 화면 `23200102` / OCR `23200102` / 일치
- 증빙일자: 화면 `2026-08-28` / OCR `20260828` / 일치
- 사용금액: 화면 `45,000` / OCR `45000` / 일치
- 최종 판정: `정상`

영수증 원본은 `%LOCALAPPDATA%\EAccAutomation\receipts`에 저장되며 외부 OCR 서비스로 전송하지 않습니다.

## 2026-08-31 보완 검증

- 숫자가 공백으로 끊긴 승인번호(예: `1828 6514`)를 한 번호로 조합한다.
- `/`를 `7`로 오인식한 승인번호(예: `3/09/115`)를 승인번호 후보로 함께 비교한다.
- `2026년 08월20일`, `2026기821`처럼 한글 또는 일부 깨진 날짜 형식을 비교한다.
- 총액이 흐린 경우 공급가액과 부가세의 합계가 화면 사용금액과 일치하면 정상으로 인정한다.
- 실제 내려받은 24·73·93·100행 영수증을 재검증하여 모두 정상으로 확인했다.

# 1단계 사용자 테스트 안내

현재 1단계 개발자 재검증은 완료되었으며 사용자 테스트와 완료 동의를 기다리고 있습니다.

## 확정된 버튼 동작

`미상신내역 불러오기`를 누르면 다음 작업을 프로그램이 연속해서 수행합니다.

1. 프로그램 전용 Edge 세션 확인
2. e-Accounting의 `미상신내역` 이동
3. 카드번호 조건 `코스트센터` 선택
4. 검색 실행
5. 화면에 표시되지 않는 방식으로 `엑셀저장` 실행
6. 다운로드 완료 감지
7. 21개 열 분석과 중복검사
8. 프로그램 표에 결과 표시

## 현재 단계의 임시 로그인 절차

로그인 자동화는 8단계 개발대상이므로 현재는 다음과 같이 시험합니다.

1. 프로그램에서 `미상신내역 불러오기`를 누른다.
2. 프로그램이 별도로 연 자동화용 Edge에서 사용자가 i-NET에 로그인한다.
3. 사용자가 `e-ACC`를 눌러 e-Accounting 화면까지 들어간다.
4. 프로그램으로 돌아와 `미상신내역 불러오기`를 다시 누른다.
5. 이후 메뉴 이동부터 데이터 표시까지 자동으로 완료되는지 확인한다.

기존에 사용자가 열어 둔 개인 Edge는 건드리지 않는다. 자동화용 Edge는 임시 세션이며 프로그램 종료 시 함께 종료된다.

## 이번 사용자 테스트 확인사항

1. 프로그램의 원본 전체 건수와 e-Accounting 검색 결과 건수가 같은지 확인한다.
2. 카드번호, 승인번호, 날짜, 금액 등 몇 개 행의 값이 같은지 확인한다.
3. 같은 결과를 다시 불러왔을 때 기존 행이 `중복`으로 표시되는지 확인한다.
4. 문제가 없으면 채팅에서 `1단계 완료 동의`라고 명시한다.

건수는 조회 시점의 미상신 상태에 따라 달라지므로 특정 숫자를 기대값으로 고정하지 않는다.

## 보조 버튼

`다운로드 파일 직접 불러오기`는 이미 내려받은 파일을 시험하거나 자동수집 장애를 우회할 때만 사용합니다. 기본 업무 흐름은 아닙니다.

# e-Acc 미상신건수 한 건씩 처리 개발 기준서

- 작성일: 2026-09-01
- 목적: e-Acc 화면을 기준으로 미상신건수를 한 건씩 검증·결재요청하고, 프로그램에는 처리 결과와 제외 사유를 남긴다.
- 적용 범위: **미상신건수 메뉴만** 대상이다. 미처리건수와 직원 알림메일은 이 개발 범위에 포함하지 않는다.
- 승인 원칙: 각 단계는 사용자 테스트와 명시적인 `완료 동의`가 있어야 다음 단계로 진행한다.

## 1. 확정된 처리 원칙

1. e-Acc가 처리의 기준 화면이며, 프로그램은 e-Acc의 현재 목록을 읽고 한 건씩 보조 처리한다.
2. 화면의 “첫 번째/두 번째 행” 번호는 처리 대상 식별값으로 사용하지 않는다. 결재 후 목록이 삭제·재정렬·동기화될 수 있으므로 `카드번호 + 승인번호 + 증빙일자 + 사용금액`으로 대상을 식별한다.
3. 프로그램은 운영체제 클립보드를 사용하지 않고 e-Acc 그리드의 현재 행 데이터를 직접 읽어 프로그램의 처리대상 행으로 만든다.
4. 처리 가능 대상은 현재 목록에 있고, 같은 실행에서 `처리 제외` 또는 `처리 완료`로 확정되지 않은 행이다.
5. 한 건의 결재요청 뒤 반드시 e-Acc 목록을 다시 검색한다. 해당 식별값이 사라진 경우에만 `처리 완료`로 기록한다.
6. 결재요청이 실패하거나 최신 목록에 그대로 남으면 자동으로 같은 행을 반복 결재요청하지 않는다. 사유와 시각을 기록하고 그 행을 이번 실행의 `처리 제외` 대상으로 둔다.
7. 영수증 OCR·방향보정·승인번호/증빙일자/사용금액 비교 코드는 이미 시험한 로직을 그대로 유지한다. 이 개발에서는 그 결과를 한 건 처리 흐름에 연결할 뿐, 판독 규칙을 변경하지 않는다.
8. 업종에 `PG일반`이 포함되는 행은 기존 방식대로 영수증에서 사업자번호를 추출하고 비즈노에서 상호를 조회한다. 조회 성공 시 실구매처 팝업에 값 자동 입력·등록을 완료한 뒤에만 결재요청 단계로 진행한다.
9. OCR 이상, 증빙 미동기화, PG 조회 실패, 실구매처 등록 실패는 자동 결재요청하지 않는다. 각각 `처리 제외`와 구체적 사유로 기록한다.
10. 계정별 규칙은 영수증 OCR 결과와 화면 데이터를 함께 확인한 뒤 결재선 지정 전에 적용한다. `예외`는 결재선 지정으로 넘어가지 않으며 `처리결과=예외처리`와 구체적 사유를 남긴다.

## 2. 최종 자동 처리 흐름

```text
i-NET 로그인
  → 최근소식 팝업이 있으면 즉시 닫기
  → e-Acc 아이콘 클릭
  → 미상신건수 메뉴 이동
  → 카드번호 조건 = 코스트센터 → 검색
  → 현재 첫 처리 가능 행을 e-Acc 그리드에서 읽기
  → 영수증 OCR 검증
      ├─ PG일반: 사업자번호 OCR → 비즈노 조회 → 실구매처 자동등록
      ├─ 계정별 규칙 검증
      │   ├─ 예외: 예외처리·사유 기록 → 다음 처리 가능 행
      │   └─ 정상: 해당 행 체크 → 결재선 지정 → 결재요청
      │          → 목록 재검색 → 행 소멸 확인 → 처리 완료 기록
      └─ 이상/실패: 처리 제외 및 사유 기록
  → 다음 처리 가능 행으로 반복
  → 처리 가능 행이 없으면 실행 요약 표시
```

## 3. 단계별 개발 및 승인

| 단계 | 개발내용 | 상태 | 다음 단계 진행 조건 |
|---:|---|---|---|
| 1 | 로그인·팝업닫기·미상신 검색 및 현재 행 읽기 | 승인 완료 | 2026-09-01 사용자 테스트 및 완료 동의 |
| 2 | 한 건 영수증 OCR·PG 조회 결과를 처리대상에 연결 | 승인 완료 | 2026-09-01 사용자 테스트 및 완료 동의 |
| 3 | 한 건 체크·결재선 지정·결재요청 및 결과 확인 | 사용자 테스트 중 | 2단계 승인 완료 |
| 4 | 실패 제외·목록 재조회·다음 행 반복 | 시작 전 | 3단계 승인 완료 |
| 5 | 처리결과 이력·실행 요약·안전중지 | 시작 전 | 4단계 승인 완료 |
| 6 | 실제 업무 데이터 연속처리 운영 검증 | 시작 전 | 5단계 승인 완료 |

### 현재 반영한 계정별 규칙 (2026-09-04)

| 계정명 일치 조건 | 판정 | 예외 사유 |
|---|---|---|
| `특근자식비` | 15,000원 이상인 경우 `ceil(사용금액 / 15,000)`명 이상의 **직원 명단 성명**이 적요에 있어야 함. 같은 성명은 한 명으로 계산 | 특근자식비 사용인원 불충족 |
| `차량유지비-유류대(법인)`·`차량유지비-주차비`·`차량유지비-세차비`·`차량유지비-통행료` | OCR에서 차량비 분류 단어가 발견되고 계정과 다르면 예외. 비교 단어를 찾지 못하면 정상 | 계정과 상이한 영수증 첨부 |
| `일반복리비-현장지원 (현장대리인 활동지원 식음료대)` | 200,000원 초과, 적요 비공백, OCR 주류 문구 중 하나라도 있으면 예외 | 현장대리인 사용금액 초과 / 불필요한 적요 작성 / 영수증에 주류포함 |
| `회의비`·`부서회의비`·`업무회의비`·`일반복리비` | 화면 데이터만으로 즉시 예외. 유사 계정은 적용하지 않음 | 계정명별 지정 문구 |

직원 성명은 암호화된 `config/mail_recipients.eacc`에서 성명만 메모리로 읽고, 이메일주소는 계정별 검증에 사용하거나 표시하지 않는다.

---

## 1단계: 로그인·팝업닫기·미상신 검색 및 현재 행 읽기

### 개발내용

- 프로그램에서 입력받은 i-NET 아이디·비밀번호로 로그인하고, 요청 직후 화면 입력값을 지운다. 로그인정보는 저장하지 않는다.
- 로그인 완료 화면에서 `최근소식` 팝업이 존재하면 제공된 `closeAlarmPopup()` 닫기 동작을 우선 실행한다. 팝업이 없으면 기다리지 않고 다음 단계로 진행한다.
- e-Acc 아이콘을 클릭하고 미상신건수 메뉴로 이동한다.
- 카드번호 조건을 `코스트센터`로 선택하고 검색 완료를 확인한다.
- e-Acc 현재 결과 그리드에서 각 행의 업무 필드를 읽는다. 첫 행만 화면 위치로 선택할 수 있으나, 프로그램 내부에서는 즉시 거래 식별값을 생성한다.
- 검색 결과가 0건이면 오류가 아니라 `처리 대상 없음`으로 기록하고 정상 종료한다.

### 사용자 테스트

1. 서로 다른 두 계정으로 각각 실행하여, 이전 계정의 로그인정보가 재사용되지 않는지 확인한다.
2. 최근소식 팝업이 있는 경우와 없는 경우 모두 e-Acc 아이콘을 클릭하는지 확인한다.
3. 코스트센터 검색 결과의 첫 행에서 카드번호·승인번호·증빙일자·사용금액·계정명·업종이 프로그램의 현재 대상 정보와 같은지 확인한다.
4. 검색 결과가 없는 기간 또는 조건에서는 오류창 없이 `처리 대상 없음`으로 끝나는지 확인한다.

### 완료 기준

- 로그인정보가 파일·DB·로그·브라우저 저장소에 남지 않는다.
- 팝업 유무와 관계없이 e-Acc 진입·검색이 완료된다.
- 현재 행의 식별값과 주요 필드가 e-Acc 화면과 일치한다.
- 실제 결재요청은 아직 실행하지 않는다.

### 승인

- [x] 개발자 자체검증 완료
- [x] 사용자 테스트 완료
- [x] 사용자 완료 동의 (2026-09-01)

---

## 2단계: 한 건 영수증 OCR·PG 조회 연결

### 개발내용

- 1단계에서 식별된 한 건의 영수증을 열어 기존 OCR 검증 모듈을 실행한다.
- 현재 첫 처리대상을 읽으면 이 검증을 자동 시작하며, 자동 검증 중에는 원본 영수증 뷰어를 띄우지 않고 판정값과 사유만 프로그램 표·이력에 표시한다. 원본 확인은 기존 수동 검증 버튼으로 수행한다.
- `증빙유무 #`은 영수증 동기화 완료 상태로 검증한다. `Y`는 동기화 대기, `N`은 사용자 미등록 상태로 처리 제외 사유를 남긴다.
- 업종에 `PG일반`이 포함된 경우에만 사업자번호 추출·비즈노 상호조회를 실행한다.
- PG 조회 성공 시 해당 행의 실구매처 등록 팝업을 자동으로 열고 사업자번호·상호를 자동 입력·등록한다.
- OCR·조회·등록 결과를 한 거래 이력으로 남긴다.

### 사용자 테스트

1. 일반 행 한 건에서 승인번호·증빙일자·금액 판정 결과를 확인한다.
2. `PG일반(인증)` 또는 `PG일반(비인증)` 행 한 건에서 사업자번호·비즈노 상호·실구매처 등록값을 확인한다.
3. OCR 이상·증빙 `Y`·증빙 `N`·비즈노 조회실패가 결재요청 대상에서 제외되는지 확인한다.

### 완료 기준

- 기존 OCR 판정 결과가 변경되지 않는다.
- PG가 아닌 행에 외부 상호조회를 실행하지 않는다.
- 이상·미확정 행은 다음 결재요청 단계로 넘어가지 않는다.

### 승인

- [x] 개발자 자체검증 완료
- [x] 사용자 테스트 완료
- [x] 사용자 완료 동의 (2026-09-01)

---

## 3단계: 한 건 결재요청 및 결과 확인

### 개발내용

- 검증을 통과한 현재 한 건만 e-Acc 그리드에서 체크한다.
- `결재선 지정`을 클릭해 새 폼을 연다.
- `선택 행 결재요청`은 검증을 통과한 한 건만 결재선 지정과 결재요청을 연속 실행한다. 프로그램의 별도 중간 확인창은 표시하지 않는다.
- e-Acc가 추가로 `결재요청 하시겠습니까?` 브라우저 확인창을 표시하면, 프로그램은 그 확인창만 자동으로 `확인` 처리한다.
- e-Acc가 `이미 다른 사용자가 처리중입니다.`를 알리면 해당 알림을 자동 확인하고, 남아 있는 결재선 지정 창과 e-Acc 선택을 닫은 뒤 `예외처리`로 기록한다. 이 행은 같은 실행에서 재결재요청하지 않는다.
- 사용자만 경고창에서 `예`를 선택한 경우에만 열린 같은 폼의 `결재요청`을 클릭한다. 프로그램은 경고창을 거치지 않은 자동 결재요청 또는 같은 팝업의 재전송을 하지 않는다.
- 팝업 메시지·창 닫힘·목록 재검색을 통해 결재요청 결과를 확인한다.
- 목록에서 해당 식별값이 사라진 경우만 `처리 완료`로 기록한다.

### 사용자 테스트

1. 테스트용 정상 행 한 건만 결재요청되는지 확인한다.
2. 결재요청 후 해당 행이 목록에서 삭제되고 이력에 `처리 완료`가 남는지 확인한다.
3. 결재요청 실패 또는 보류 상황에서 `처리 완료`로 잘못 기록하지 않는지 확인한다.

### 완료 기준

- 한 번의 실행에서 여러 행이 동시에 체크되지 않는다.
- 같은 행에 중복 결재요청을 보내지 않는다.
- 결재 결과는 재검색 결과로 확인한다.

### 승인

- [ ] 개발자 자체검증 완료
- [ ] 사용자 테스트 완료
- [ ] 사용자 완료 동의

---

## 4단계: 실패 제외·다음 행 반복

### 개발내용

- 결재요청 실패·목록 유지·OCR 이상·PG 처리 실패 행을 이번 실행에서 제외한다.
- 목록을 매 건 재검색한 뒤 제외되지 않은 첫 처리 가능 행을 찾아 다음 건을 처리한다.
- 제외 키, 제외 사유, 시각, 재시도 횟수를 남긴다.
- 처리 가능 행이 없을 때만 반복을 종료한다.

### 사용자 테스트

1. 첫 행을 의도적으로 처리 불가 상태로 두고, 다음 행으로 진행하는지 확인한다.
2. 처리 불가 행이 화면 재정렬 뒤에도 다시 선택되지 않는지 확인한다.
3. 중간에 새 행이 동기화되어도 이미 제외·완료된 행을 중복 처리하지 않는지 확인한다.

### 완료 기준

- 화면 행 번호가 바뀌어도 잘못된 행을 처리하지 않는다.
- 한 행 실패가 전체 실행을 중단시키지 않는다.
- 무한 재시도 없이 안전하게 종료한다.

### 승인

- [ ] 개발자 자체검증 완료
- [ ] 사용자 테스트 완료
- [ ] 사용자 완료 동의

---

## 5단계: 처리결과 이력·실행 요약·안전중지

### 개발내용

- 거래별 상태를 `검증 정상`, `검증 이상`, `PG 조회 완료`, `실구매처 등록 완료`, `결재요청`, `처리 완료`, `처리 제외`로 이력화한다.
- 실행 종료 시 처리 완료·처리 제외·미처리·오류 건수와 대상 목록을 보여 준다.
- 사용자가 언제든 중지할 수 있고, 중지 시 현재 작업을 안전하게 마무리한 뒤 이후 행의 처리만 멈춘다.
- 다음 실행에는 이전 실행의 완료·제외 이력을 보여 주되, e-Acc의 최신 목록을 다시 기준으로 판단한다.

### 사용자 테스트

1. 정상·이상·제외·완료 행의 이력 내용이 실제 e-Acc 결과와 일치하는지 확인한다.
2. 중지 후 재실행해도 이미 완료된 행에 결재요청하지 않는지 확인한다.
3. 실행 요약의 건수와 e-Acc 최신 목록을 대조한다.

### 완료 기준

- 결과 이력만으로 각 행의 처리 경과를 확인할 수 있다.
- 중지·오류·재시작 상황에서 중복 결재요청이 없다.

### 승인

- [ ] 개발자 자체검증 완료
- [ ] 사용자 테스트 완료
- [ ] 사용자 완료 동의

---

## 6단계: 실제 업무 데이터 연속처리 운영 검증

### 개발내용

- 승인된 1~5단계 동작을 실제 업무 범위에서 제한된 건수부터 연속 처리한다.
- 초기 운영에서는 건수 제한과 건별 확인 옵션을 유지한다.
- 결과가 안정화된 후 사용자 승인에 따라 무확인 연속처리 여부를 결정한다.

### 승인

- [ ] 개발자 자체검증 완료
- [ ] 사용자 테스트 완료
- [ ] 사용자 완료 동의

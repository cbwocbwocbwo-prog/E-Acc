# 8단계 i-NET 로그인 자동화 사용자 테스트 안내

개발자 자체검증은 완료했습니다. 실제 i-NET 계정으로 로그인되는지는 사용자가 직접 확인하고 완료에 동의해야 합니다.

## 테스트 방법

1. [run_app.bat](run_app.bat)를 실행한다.
2. 화면 상단 `i-NET 로그인 (현재 실행에서만 사용)` 영역에 아이디와 비밀번호를 입력한다.
3. 비밀번호가 별표(`*`)로만 보이는지 확인한다.
4. `미상신내역 불러오기`를 한 번 누른다.
5. `최근소식` 팝업이 있으면 즉시 닫고, 없으면 기다리지 않고 메인 화면의 `e-Acc` 아이콘을 자동 클릭하는지 확인한다.
6. Edge의 번역 안내와 암호 저장 안내가 나타나지 않는지 확인한다.
7. e-Accounting에서 검색 결과가 0건인 경우 프로그램에 오류창이 뜨지 않고 `검색 결과 없음`으로 표시되는지 확인한다.
8. 로그인 입력칸이 비어 있는지 확인한다.
9. 프로그램을 종료하지 않은 상태에서 `선택 행 영수증 검증` 또는 `# 전체 영수증 OCR 검증`을 실행한다. 다시 로그인 화면이나 아이디·비밀번호 입력 요구 없이 진행되는지 확인한다.
10. 프로그램을 완전히 종료한 뒤 다른 아이디로 다시 실행한다. 두 로그인 입력칸이 비어 있고 새로 입력한 아이디로 로그인되는지 확인한다.

## 보안 확인사항

- i-NET의 기본 `아이디 저장` 옵션을 자동으로 해제한다.
- 자동화용 Edge의 `암호를 저장하시겠습니까?` 안내가 나타나지 않는다.
- 아이디·비밀번호는 파일, SQLite DB, 로그, 명령행 인자에 저장하지 않는다.
- 입력값은 자동화 요청을 시작한 직후 프로그램 화면에서 제거한다.
- 로그인 성공 뒤에는 임시 Edge 세션의 인증 쿠키만 프로그램 종료 전까지 사용한다. 프로그램 종료 시 임시 Edge 프로필과 함께 제거된다.

## 로그인 실패 시

아이디·비밀번호가 틀렸거나 계정 상태(잠김·비밀번호 변경 필요 등) 때문에 로그인에 실패하면 입력란에 다시 입력한 후 재시도한다. 실패한 입력값은 화면에 남지 않는다.

## 완료 동의

위 항목이 정상이라면 채팅에 `8단계 완료 동의`라고 알려 주세요. 문제가 있으면 자동화용 Edge에 표시된 화면 또는 프로그램 메시지를 캡처해 주시면 보완하겠습니다.

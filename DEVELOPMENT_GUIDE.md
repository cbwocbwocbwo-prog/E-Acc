# E-Acc 법인카드 자동처리 개발 기준서

- 문서 목적: 개발자와 사용자가 합의한 요구사항, 개발 순서, 테스트 방법 및 단계별 승인 상태를 관리한다.
- 최초 작성일: 2026-08-28
- 개발 순서: **미상신건수 → 미처리건수 → 로그인 및 e-ACC 진입 자동화 → 배포**
- 핵심 원칙: **각 개발 단계는 사용자 테스트와 명시적인 완료 동의가 있어야 다음 단계로 진행할 수 있다.**

## 1. 단계 진행 규칙

### 1.1 단계 상태

각 단계는 다음 상태 중 하나만 가진다.

- `시작 전`: 개발하지 않은 상태
- `개발 중`: 해당 단계만 개발 중인 상태
- `사용자 테스트 중`: 개발이 끝나 사용자가 시험하는 상태
- `보완 중`: 사용자 시험에서 발견된 문제를 수정하는 상태
- `승인 완료`: 사용자가 완료에 동의한 상태
- `중단`: 사용자 결정 또는 외부 문제로 진행을 멈춘 상태

### 1.2 승인 게이트

1. 개발자는 해당 단계의 완료 기준을 모두 확인한다.
2. 사용자에게 테스트 방법과 결과를 제공한다.
3. 사용자가 직접 테스트한다.
4. 문제점이 있으면 같은 단계에서 수정과 재시험을 반복한다.
5. 사용자가 명시적으로 `완료 동의`한 경우에만 상태를 `승인 완료`로 변경한다.
6. 이전 단계가 `승인 완료`가 아니면 다음 단계를 시작하지 않는다.
7. 승인된 요구사항이 나중에 변경되면 영향받는 단계의 승인을 다시 받아야 한다.

### 1.3 단계 승인 현황

| 단계 | 개발내용 | 상태 | 사용자 테스트일 | 사용자 승인일 | 비고 |
|---:|---|---|---|---|---|
| 1 | 공통 기반 및 미상신내역 불러오기 | 완료 | 2026-08-31 | 2026-08-31 | 사용자 완료 동의 |
| 2 | 영수증 열기 및 기본 OCR 검증 | 보완 보류 |  |  | OCR 정확도는 이후 보완하기로 사용자 결정 |
| 3 | PG일반 사업자번호 및 외부 상호조회 | 개발 중 |  |  | 선택 행 사업자번호 OCR·비즈노 조회·실구매처 등록 연동 구현 중 |
| 4 | 계정명별 동적 규칙관리 | 시작 전 |  |  |  |
| 5 | 재검증 및 미상신 검토화면 | 시작 전 |  |  |  |
| 6 | 전표처리 및 미상신건수 메뉴 완료 | 시작 전 |  |  |  |
| 7 | 미처리건수 사용자별 알림 | 시작 전 |  |  |  |
| 8 | i-net 로그인 및 e-ACC 진입 자동화 | 승인 완료 | 2026-09-08 | 2026-09-08 | 미상신내역 로그인·팝업 처리·e-ACC 진입 자동화 사용자 완료 동의 |
| 9 | 배포 및 최종 운영 검증 | 시작 전 |  |  |  |

### 1.4 웹 UI 운영 관찰 항목 (2026-09-09)

- 현재 웹 UI(pywebview)는 Tkinter 백엔드 상태를 0.4초 주기로 동기화한다.
- 이 방식은 현재 기능 범위에서 유지한다. 행 수가 많은 실제 운영 데이터에서 화면 반응, 가로 스크롤 유지, 메모리 사용 문제가 확인될 때만 동기화 방식 또는 주기를 보완한다.
- 현 시점에서는 성능 최적화 개발을 진행하지 않으며, 운영 테스트에서 문제가 재현된 경우에만 별도 승인 후 수정한다.

### 1.5 웹 UI 고정 런타임 배포 원칙 (2026-09-09)

- 새 웹 UI의 공식 실행 엔진은 Microsoft Edge WebView2 Fixed Version Runtime x64 `152.0.4191.62`이다.
- 개발 PC와 배포 PC 모두 프로젝트의 `runtime/webview2_fixed_x64/Microsoft.WebView2.FixedVersionRuntime.152.0.4191.62.x64` 폴더를 사용한다. Windows에 별도로 설치된 Edge 또는 Evergreen WebView2 버전은 사용하지 않는다.
- `eacc_app/ui.py`는 pywebview의 `WEBVIEW2_RUNTIME_PATH`를 위 포함 경로로 고정한다. 포함 실행 파일이 없으면 정상 배포물이 아니므로 실행을 중단해야 한다.
- 배포 패키지에는 위 고정 런타임 폴더의 548개 파일 전체를 포함한다. 현재 용량은 약 798MB이며, 일부 파일만 선택해 배포하지 않는다.
- Windows 10 비패키지 배포 시에는 Microsoft 공식 Fixed Version Runtime 권한 요구사항을 설치 절차에 포함하고, 실제 운영 PC에서 새 UI 표시를 확인한다.

## 2. 전체 업무 흐름

```text
법인카드 사용
  ├─ 사용자가 O-Park에 사용내역 등록
  │    └─ e-Accounting 미상신건수로 이동
  │         └─ 영수증·PG·계정별 규칙 검증
  │              ├─ 정상 → 사용자 검토 → 전표처리
  │              └─ 이상 → 이상 사유 표시 → 팀 전표담당자 메일자료
  │
  └─ 사용자가 O-Park에 사용내역 미등록
       └─ e-Accounting 미처리건수에 잔존
            └─ 성명·부서별 분리
                 └─ 개인별 Excel 및 Outlook 알림메일
```

## 3. 확정된 공통 요구사항

### 3.1 프로그램 운영 원칙

- e-Acc를 처리 기준 화면으로 사용한다. 프로그램은 `선택 1건 검증 → 결과 이력 → 결재 후 목록 재조회`만 보조하며 일괄 결재 요청을 하지 않는다.
- 판정이 불명확하거나 외부 조회가 실패한 건은 자동 통과시키지 않는다.
- 이미 처리된 거래를 다시 처리하지 않도록 중복방지 키와 처리이력을 관리한다.
- 각 판정에는 사용한 규칙 버전, 판정시각, 사용자 작업 및 처리결과를 남긴다.
- 오류가 발생한 행 때문에 나머지 행의 처리가 중단되지 않도록 행 단위로 실패를 분리한다.
- 사용자가 e-Acc에서 한 건의 결재 요청을 마친 뒤 프로그램에서 목록 재조회를 실행한다. 해당 거래가 최신 목록에서 사라졌을 때만 `처리 완료`로 기록한다.
- 목록에 계속 남아 있으면 `목록 유지`로 기록하며, 기존 행 번호·화면 위치를 다음 거래에 재사용하지 않는다.

### 3.2 원본 Excel 처리

- `미상신내역.xls`와 `미처리내역.xls`는 확장자는 `.xls`이지만 내부는 UTF-8 HTML 표 형식이다.
- 카드번호, 승인번호, 사업자번호는 선행 0과 전체 자릿수를 보존하기 위해 문자열로 읽는다.
- 날짜 형식은 내부적으로 `YYYY-MM-DD`로 정규화한다.
- 화면 버튼에 해당하는 `선택`, `매출전표` 등의 이미지 경로는 업무 데이터에서 제외한다.
- 생성하는 사용자별 첨부파일은 실제 `.xlsx` 형식으로 만든다.
- 거래 중복판정 기본키는 다음과 같다.
  - 미상신건수: `카드번호 + 승인번호 + 증빙일자 + 사용금액`
  - 미처리건수: `카드번호 + 승인번호 + 이용일자 + 사용금액`

#### 3.2.1 미상신내역 실제 다운로드 절차

- `미상신내역 불러오기`는 파일 선택창을 표시하지 않고 e-Accounting 조회부터 다운로드와 프로그램 표시까지 자동 수행한다.
- 현재 1단계에서는 프로그램이 임시 프로필의 자동화용 Edge를 열며, 사용자는 i-NET 로그인과 `e-ACC` 진입까지만 수동으로 수행한다.
- 8단계에서는 아이디·비밀번호 입력부터 e-Accounting 진입까지 연결하여 완전한 한 번 클릭 흐름으로 완성한다.
- 실제 화면 취득 절차는 다음과 같다.
  1. 로그인된 `https://eaccounting.skons.co.kr/` 화면을 연다.
  2. 왼쪽 현황의 `미상신건수` 또는 왼쪽 메뉴의 `미상신내역`을 선택한다.
  3. 카드번호 드롭다운에서 `코스트센터`를 선택한다.
  4. `검색` 버튼을 눌러 결과를 표시한다.
  5. 결과 영역을 마우스 오른쪽 버튼으로 누르고 사용자 정의 메뉴의 `엑셀저장`을 선택한다.
- 2026-08-28 실제 화면에서 확인한 주요 선택자는 다음과 같다.
  - 미상신건수: `#body > table.my_info.mgt-5 > tbody > tr:nth-child(3) > td:nth-child(1) > a`
  - 카드번호 영역: `#CARD_NO_LIST`
  - 실제 드롭다운: `select#CARD_NO`, 코스트센터 옵션값 `C`
  - 검색 버튼: `body > form:nth-child(3) > table > tbody > tr > td > table.board-search > tbody > tr:nth-child(1) > td:nth-child(7) > div > a > span`
- 위치 기반 선택자는 화면 개편에 취약하므로 이후 자동화에서는 고정 ID와 표시문구를 우선하고, 제공된 전체 선택자는 보조 수단으로 사용한다.
- 실제 다운로드 파일명은 `ExcelData.xls`이며 기존 `미상신내역.xls`와 동일한 UTF-8 HTML 표 형식임을 확인했다.
- 사용자가 이미 내려받은 파일을 시험하거나 자동수집 장애를 우회할 수 있도록 `다운로드 파일 직접 불러오기`를 별도 보조 기능으로 둔다.

### 3.3 영수증 기본 검증

- 최초 판정은 모든 거래에 영수증 이미지가 필요하다는 전제로 시작한다.
- `증빙유무 = #`은 영수증 이미지가 있는 상태다.
- `증빙유무 = N 또는 Y`는 영수증 이미지가 없는 상태다.
- 영수증에서 다음 값을 추출하고 화면 행과 비교한다.
  - 승인번호
  - 증빙일자
  - 사용금액
- 비교 전 공백, 하이픈, 쉼표, 통화기호 및 날짜 형식을 정규화한다.
- 세 값은 정규화 후 정확히 일치해야 한다.
- 값 누락, 복수 후보, 낮은 OCR 신뢰도 또는 불일치는 구체적인 사유와 함께 이상 또는 확인필요로 처리한다.

### 3.4 영수증 불필요 재검증

- 영수증이 없는 건은 최초 검증에서 `영수증 미첨부`로 표시한다.
- 사용자는 행별로 `영수증 불필요` 체크만 할 수 있다.
- 별도의 사유 입력란은 만들지 않는다.
- 사용자가 재검증을 실행하면 영수증 관련 검사만 생략하고 계정별 나머지 규칙은 모두 실행한다.
- 재검증을 통과한 건은 `정상(영수증 불필요)`로 표시한다.
- 체크 사용자, 체크시각, 재검증시각 및 최종 처리결과는 자동으로 기록한다.
- 영수증이 있다고 표시되지만 뷰어가 열리지 않는 경우는 영수증 면제가 아닌 시스템 오류로 처리한다.

### 3.5 PG일반 외부 상호조회

- 업종 문자열에 `PG일반`이 포함된 모든 행이 외부 상호조회 대상이다.
- `PG일반(인증)`, `PG일반(비인증)` 및 향후 추가되는 `PG일반...` 형식을 모두 포함한다.
- PG일반 대상 행만 영수증에서 사업자번호를 추가로 추출한다.
- 사업자번호는 숫자 10자리와 체크섬을 검사한다.
- 공급자, 가맹점, 판매자 또는 사업자등록번호 주변의 번호를 우선 후보로 사용한다.
- 실제 공급자 사업자번호를 확정할 수 없으면 자동 통과시키지 않는다.
- 사업자번호는 비즈노 웹사이트 `https://bizno.net/`의 사업자번호 조회에 사용한다.
- 프로그램 실행 즉시 사용자 화면에 보이지 않는 백그라운드 웹 세션을 미리 연결해 최초 조회 지연을 줄인다.
- 비즈노 공식 API는 사용하지 않는다.
- 내부 가맹점 마스터나 과거 조회결과를 상호 판정에 사용하지 않는다.
- 같은 실행 작업 안에서 동일 사업자번호가 반복되면 1회 조회결과를 같은 작업 안에서만 공유할 수 있다.
- 조회 실패, 미등록 또는 자동화 차단 시 `외부 상호조회 실패`로 처리하며 임의 상호를 사용하지 않는다.
- 비즈노 조회 상호와 화면 거래처가 다른 것만으로 이상 처리하지 않는다.

### 3.6 계정명별 동적 규칙

- 계정별 판정조건은 코드에 하드코딩하지 않고 프로그램의 규칙관리 화면에서 관리한다.
- 사용자는 계정명을 선택한 뒤 규칙을 추가, 수정, 비활성화 및 순서 변경할 수 있다.
- 삭제는 감사이력 보존을 위해 물리삭제 대신 비활성화를 기본으로 한다.
- 기본 연산자는 다음을 지원한다.
  - 값 있음/없음
  - 같음/다름
  - 포함/미포함
  - 시작문자/끝문자
  - 금액 이상/이하/범위
  - 날짜 차이
  - 목록 중 하나와 일치
  - 다른 필드와 비교
  - 정규표현식 패턴
  - OCR 또는 외부조회 결과 비교
- 조건은 AND/OR로 묶을 수 있어야 한다.
- 규칙 변경은 `작성 중 → 시험 → 적용` 순서로 진행한다.
- 진행 중인 작업에는 작업 시작 시점의 규칙 버전을 끝까지 적용한다.
- 지원하지 않는 새로운 데이터나 연산 방식은 개발자가 새 규칙 유형으로 추가한다.

### 3.7 Outlook 메일 공통 원칙

- 개발환경에서는 실제 발송하지 않고 Outlook 임시보관함에 초안만 만든다.
- 개발메일 제목에는 `[TEST]`를 표시한다.
- 개발버전에서는 Outlook `Send` 호출을 금지한다.
- 메일 수신자, 제목, 본문, 첨부파일 및 그룹분리 결과를 사용자가 확인한다.
- 실제 발송 방식, 메일 제목과 본문, 참조/숨은참조 및 재발송 정책은 해당 단계 개발 중 사용자와 확정한다.

### 3.8 직원 메일정보

- 원본 직원정보는 `성명/이름`, `부서명/팀`, `이메일주소/이메일` 세 항목만 사용한다.
- 메일주소 연결 순서는 다음과 같다.
  1. 성명을 정규화하여 일치시킨다.
  2. 해당 성명에 이메일이 하나뿐이면 바로 연결한다.
  3. 동일 성명에 이메일이 여러 개면 메일정보의 팀 문자열이 화면 부서명에 포함되는지 비교한다.
  4. 정확히 하나의 후보만 남으면 연결한다.
  5. 후보가 없거나 여러 개면 메일을 만들지 않고 확인대상으로 표시한다.
- 직원정보 원본 Excel은 배포 폴더에 포함하지 않는다.
- 배포용 직원정보는 AES-256-GCM 방식의 암호화 파일로 만든다.
- 프로그램은 실행 중 메모리에서만 직원정보를 복호화하며 평문 파일을 생성하지 않는다.
- 암호화 파일이 변조되거나 손상되면 사용을 중단하고 오류를 표시한다.
- 동일한 배포용 암호화 파일은 다른 PC와 Windows 계정에서도 사용할 수 있게 한다.
- 암호화 파일 직접 열람 방지를 목적으로 하며, 로컬 관리자나 전문적인 프로그램 역분석까지 완전히 방지하는 구조는 아님을 전제로 한다.

### 3.9 로그인정보 보안

- 로그인 자동화는 두 메뉴 개발과 사용자 승인이 끝난 후 진행한다.
- 프로그램 실행 때마다 사용자가 아이디와 비밀번호를 직접 입력한다.
- 아이디와 비밀번호를 파일, DB, 레지스트리, 환경변수 또는 Windows 자격증명 관리자에 저장하지 않는다.
- 브라우저 자동완성 및 비밀번호 저장 기능을 사용하지 않는다.
- 로그인정보를 로그, 오류 메시지, 화면 캡처 또는 명령행 인자에 기록하지 않는다.
- 로그인 과정에서만 메모리에 임시 보관하고 성공·실패 또는 프로그램 종료 시 제거한다.

## 4. 단계별 개발 및 사용자 승인

## 1단계: 공통 기반 및 미상신내역 불러오기

### 개발내용

- 프로그램 기본 창, 메뉴, 진행상태 및 오류표시 구성
- 자동화용 Edge 실행 및 로그인된 e-Accounting 세션 확인
- 미상신 메뉴 이동, 코스트센터 조건 선택, 검색 및 Excel 자동 다운로드
- 미상신내역 `.xls` 입력 및 21개 열 구조 파싱
- 식별자 문자열 보존과 날짜·금액 정규화
- 거래 중복검사 및 작업이력 저장 기반 구성
- 원본 행과 프로그램 행을 연결하는 내부 거래 ID 구성

### 사용자 제공자료

- 최신 미상신내역 다운로드 파일
- 화면과 파일의 동일 행을 확인할 수 있는 사례

### 사용자 테스트

- `미상신내역 불러오기` 클릭 후 메뉴 이동·검색·다운로드·표시 자동처리 확인
- 원본 전체 건수와 프로그램 표시 건수 비교
- 카드번호, 승인번호, 날짜, 금액 및 계정명 표본 대조
- 승인번호 선행 0 보존 확인
- 중복 파일 재입력 시 중복표시 확인

### 완료 기준

- 원본 행 누락과 값 변형이 없다.
- 중복판정이 재현 가능하다.
- 오류가 발생한 행과 정상 행을 분리할 수 있다.

### 승인

- [x] 개발자 자체검증 완료
- [x] 사용자 테스트 완료
- [x] 사용자 보완요청 반영 완료
- [x] 사용자 완료 동의

개발자 자체검증 기록(2026-08-28):

- Edge의 실제 e-Accounting 화면에서 `코스트센터` 선택과 검색결과 62건 확인
- 우클릭 `엑셀저장`으로 `ExcelData.xls` 다운로드 확인
- 21개 열, 62개 행 모두 파싱 성공 및 오류 0건
- 선행 0 승인번호 9건 보존, 첫 행 `08105102` 확인
- 최초 입력 신규 62건, 동일 파일 재입력 중복 62건 확인
- 기존 제공본 `미상신내역.xls`와 다운로드 파일의 SHA-256 해시가 동일함을 확인
- 사용자 피드백으로 기본 버튼을 파일 선택 방식에서 e-Accounting 자동수집 방식으로 변경했다.

개발자 자동수집 통합검증 기록(2026-08-31):

- 로그인된 자동화용 Edge에서 법인카드 전표처리 메뉴와 `미상신내역`으로 자동 이동했다.
- 카드번호 조건을 `코스트센터`로 선택하고 검색 완료를 기다린 뒤 화면을 노출하지 않는 방식으로 `엑셀저장`을 실행했다.
- 조회 시점 원본 137건을 다운로드했으며 21개 열, 137개 행 모두 파싱 성공, 오류 0건을 확인했다.
- 기존 DB와 비교해 신규 83건, 중복 54건으로 분류되고 프로그램 표에 137건이 표시됨을 확인했다.
- 건수는 실제 업무 상태에 따라 변하므로 프로그램은 조회 결과를 동적으로 표시하며 특정 건수를 기준값으로 고정하지 않는다.

---

## 2단계: 영수증 열기 및 기본 OCR 검증

### 개발내용

- 미상신 행 선택 및 영수증 별도 뷰어 실행
- 뷰어 창 식별, 이미지 로딩확인 및 원본 저장 또는 지정영역 캡처
- 승인번호, 증빙일자 및 사용금액 OCR
- 화면값과 OCR값 비교 및 필드별 이상 사유 생성
- 뷰어 실패, 이미지 없음, OCR 실패 및 복수 후보 분리

개발 진행 기록(2026-08-31):

- 목록의 선택 행을 카드번호·승인번호·증빙일자·사용금액으로 웹 그리드의 단일 행과 연결했다.
- 해당 행만 선택해 결재선 지정 창을 열고 `imageframe`의 썸네일 주소에서 `DocIRN`, `Idx`, `UserID`, `CorpNo`를 추출했다.
- `ImgType=original`과 운영 서버의 `orignal`이 같은 원본을 반환함을 확인했으며 프로그램은 표준 표기 `original`을 사용한다.
- 로그인 ID를 별도로 저장하거나 가공하지 않고 현재 로그인 세션이 생성한 영수증 주소의 `UserID`를 그대로 사용한다.
- 첫 실데이터 원본 JPEG 1장(199,657바이트)을 다운로드하고 프로그램 전용 뷰어 표시를 확인했다.
- Windows 로컬 한국어 OCR로 승인번호 `23200102`, 증빙일자 `2026-08-28`, 사용금액 `45,000`을 모두 일치 판정했다.
- 원본은 변경하지 않고 0°·90°·180°·270° 메모리 회전 OCR을 지원하며 세 필드 일치도와 인식 품질로 방향을 선택한다.
- 90도 회전 표본은 270도 보정, 180도 뒤집힌 표본은 180도 보정 후 세 필드 모두 정상 판정됨을 확인했다.
- 회전 후보 점수가 같으면 자동 정상처리하지 않고 `방향 판정 불명확` 사유를 표시한다.
- 영수증 이미지는 외부 OCR 서비스로 전송하지 않는다.
- 현재 다양한 영수증 형태와 이미지 없음 사례에 대한 사용자 테스트 전 검증을 진행 중이다.

### 사용자 제공자료

- 영수증 뷰어가 열린 전체 화면 캡처
- 형태가 서로 다른 영수증 표본
- 흐림, 잘림, 복수 금액 또는 복수 승인번호 사례

### 사용자 테스트

- 정상 영수증의 세 항목 일치 확인
- 각 항목 불일치 사례 확인
- 이미지 없음과 뷰어 오류가 서로 다르게 표시되는지 확인
- 다양한 화면 배율과 창 위치에서 뷰어 제어 확인

### 완료 기준

- 행과 영수증이 잘못 연결되지 않는다.
- 불명확한 OCR 결과를 자동 정상처리하지 않는다.
- 재시도 후에도 작업이 중복되지 않는다.

### 승인

- [x] 개발자 자체검증 완료
- [x] 사용자 테스트 완료
- [x] 사용자 보완요청 반영 완료
- [x] 사용자 완료 동의 (OCR 정확도 보완은 후속 개선으로 진행)

---

## 3단계: PG일반 사업자번호 및 외부 상호조회

### 개발내용

- 업종에 `PG일반`이 포함되는지 판정
- PG 대상 영수증에서 사업자번호 후보 추출
- 사업자번호 형식과 체크섬 검증
- 프로그램 시작 시 백그라운드로 비즈노 연결을 미리 준비하고, PG 대상의 사업자번호를 비즈노에서 조회해 상호를 추출
- 복수 번호, 조회 실패 및 사이트 차단 예외 처리
- 사용자가 e-Acc의 현재 행에서 연 실구매처 등록 팝업에만 조회된 사업자번호·사업자명을 자동 입력하고 `등록`을 클릭한다. 프로그램이 행을 찾거나 팝업을 열지 않는다.
- 비즈노 조회 실패·복수 결과는 `확인 필요`로 표시하며 e-Accounting에는 등록하지 않는다.

### 사용자 제공자료

- PG일반(인증) 및 PG일반(비인증) 영수증
- 사업자번호 한 개, 여러 개 및 판독불가 사례
- 실제 비즈노 조회결과와 비교할 표본

### 사용자 테스트

- PG가 아닌 행에서 외부조회가 실행되지 않는지 확인
- PG일반 인증·비인증이 모두 조회되는지 확인
- 백그라운드 조회창이 사용자에게 노출되지 않는지 확인
- 추출번호와 비즈노 상호가 실제 결과와 같은지 확인

### 완료 기준

- 대상 업종 선택과 비대상 제외가 정확하다.
- 실패 또는 모호한 결과가 자동 통과되지 않는다.
- 사용자가 조회 브라우저를 보지 않아도 전체 작업상태를 확인할 수 있다.

### 승인

- [ ] 개발자 자체검증 완료
- [ ] 사용자 테스트 완료
- [ ] 사용자 보완요청 반영 완료
- [ ] 사용자 완료 동의

---

## 4단계: 계정명별 동적 규칙관리

### 개발내용

- 계정 선택, 규칙 추가·수정·비활성화 및 복사 기능
- 조건 연산자, AND/OR 그룹 및 이상 사유 관리
- 규칙 작성·시험·적용과 버전관리
- 변경 전후 판정결과 비교
- 사용자가 제공하는 최초 계정별 판정기준 등록

### 사용자 제공자료

- 계정명별 검사 항목
- 정상 기준과 이상 기준
- 표시할 이상 사유
- 조건 간 AND/OR 관계
- 정상·이상 실제 사례

### 사용자 테스트

- 기존 A/B 규칙 비활성화와 신규 E 규칙 추가
- 규칙 적용 전후 동일 데이터의 판정 차이 확인
- 이전 버전 복원 확인
- 진행 중인 작업의 규칙 버전 유지 확인

### 완료 기준

- 지원되는 규칙은 코드 수정 없이 사용자가 관리할 수 있다.
- 규칙 변경이 승인되지 않은 작업에 임의로 적용되지 않는다.
- 모든 판정에서 적용 규칙과 버전을 조회할 수 있다.

### 승인

- [ ] 개발자 자체검증 완료
- [ ] 사용자 테스트 완료
- [ ] 사용자 보완요청 반영 완료
- [ ] 사용자 완료 동의

---

## 5단계: 재검증 및 미상신 검토화면

### 개발내용

- 정상, 이상, 판독불가, 처리완료 및 처리실패 상태 표시
- 화면값, OCR값, 영수증 이미지, 외부조회 및 규칙결과 비교표시
- 행별 `영수증 불필요` 체크와 재검증
- 이상 사유의 실제값과 기대값 표시
- 정상 건 선택, 제외 및 재시도 기능

### 사용자 테스트

- 영수증 없는 건의 최초 이상 판정 확인
- 영수증 불필요 체크 후 사유 입력 없이 재검증
- 영수증 검사를 제외해도 계정별 규칙이 실행되는지 확인
- 체크 해제 후 다시 영수증 필수 상태가 되는지 확인

### 완료 기준

- 사용자가 한 화면에서 판정 근거를 확인할 수 있다.
- 영수증 면제가 전체 검증 면제로 오작동하지 않는다.
- 정상과 `정상(영수증 불필요)`가 구분된다.

### 승인

- [ ] 개발자 자체검증 완료
- [ ] 사용자 테스트 완료
- [ ] 사용자 보완요청 반영 완료
- [ ] 사용자 완료 동의

---

## 6단계: 전표처리 및 미상신건수 메뉴 완료

### 개발내용

- e-Acc에서 사용자가 선택한 한 건만 결재 요청
- 결재 요청 직후 최신 목록 재조회로 처리 완료 여부 확인
- 성공·실패 분리, 실패 건 재시도 및 중복처리 방지
- 이상 건을 팀별로 묶어 Excel 및 Outlook 초안 생성
- 미상신 검증부터 처리까지 전체 감사이력 저장

### 사용자 제공자료

- 정상 전표처리의 실제 클릭 순서
- 성공·실패·이미 처리됨 메시지 화면
- 팀 전표담당자 정보
- 이상 건 메일 제목, 본문 및 첨부열

### 사용자 테스트

- 한 건 결재 요청 뒤 해당 행이 최신 목록에서 `처리 완료`로 기록되는지 확인
- 이상·판독불가·미승인 건이 제외되는지 확인
- 처리 중 프로그램 종료 후 재개 확인
- 목록이 변경·추가되어도 기존 행 번호를 재사용하지 않는지 확인
- 팀별 이상 건 메일초안과 첨부파일 확인

### 완료 기준

- 사용자 승인 없는 건은 전표처리되지 않는다.
- 처리 성공과 실패가 화면 및 이력에 일치한다.
- 미상신건수의 전체 업무를 사용자가 승인한다.

### 승인 게이트

- [ ] 개발자 자체검증 완료
- [ ] 사용자 테스트 완료
- [ ] 사용자 보완요청 반영 완료
- [ ] 사용자 완료 동의
- [ ] **미처리건수 개발 시작 동의**

---

## 7단계: 미처리건수 사용자별 알림

### 개발내용

- 미처리내역 `.xls` 입력 및 23개 열 구조 파싱
- 성명·부서별 미등록 거래 그룹화
- 암호화된 직원 메일정보 로딩과 수신자 연결
- 사용자별 실제 `.xlsx` 생성
- Outlook 사용자별 메일초안과 해당 사용자 파일 첨부
- 발송이력과 재안내 대상 관리

### 사용자 제공자료

- 최신 미처리내역 다운로드 파일
- 최신 직원 메일정보 파일
- 메일 제목, 본문, 첨부열, 카드번호 마스킹 및 재발송 정책

### 사용자 테스트

- 동일 사용자의 여러 행이 한 파일로 생성되는지 확인
- 다른 사용자의 행이 섞이지 않는지 확인
- 동명이인 팀 포함관계로 올바른 이메일이 선택되는지 확인
- Outlook 초안의 수신자, 제목, 본문 및 첨부파일 확인
- 이메일 미등록·중복·미확정 사용자의 예외표시 확인

### 완료 기준

- 한 사용자 그룹당 한 개의 Excel과 한 개의 Outlook 초안이 생성된다.
- 모든 첨부파일은 해당 사용자 데이터만 포함한다.
- 개발환경에서는 실제 메일이 발송되지 않는다.
- 미처리건수의 전체 업무를 사용자가 승인한다.

### 승인

- [ ] 개발자 자체검증 완료
- [ ] 사용자 테스트 완료
- [ ] 사용자 보완요청 반영 완료
- [ ] 사용자 완료 동의

---

## 8단계: i-net 로그인 및 e-ACC 진입 자동화

### 개발내용

- 프로그램 로그인 입력창과 비밀번호 마스킹
- `https://i-net.skons.co.kr/` 로그인 자동화
- 로그인 후 선택적으로 나타나는 팝업 탐지 및 닫기
- 왼쪽 상단 `e-ACC` 아이콘 클릭
- 새로 열리는 `https://eaccounting.skons.co.kr/` 창 식별
- 기존 미상신건수·미처리건수 모듈과 연결
- 브라우저 세션 종료와 로그인정보 제거

### 사용자 제공자료

- 로그인 화면, 팝업 및 e-ACC 아이콘 화면 캡처
- 로그인 성공·실패 시 화면과 메시지
- 실제 메뉴 진입 순서

### 사용자 테스트

- 팝업이 있는 경우와 없는 경우 모두 시험
- 잘못된 아이디·비밀번호 입력 후 재시도 확인
- 로그인정보가 재실행 후 남지 않는지 확인
- e-ACC 창을 팝업으로 오인하여 닫지 않는지 확인
- 로그인부터 두 메뉴 실행까지 전체 흐름 확인

### 완료 기준

- 로그인정보가 영구 저장되거나 로그에 노출되지 않는다.
- 팝업 유무와 관계없이 e-Accounting 창에 도달한다.
- 로그인 자동화가 기존 메뉴 기능의 안정성을 훼손하지 않는다.

### 승인

- [x] 개발자 자체검증 완료
- [x] 사용자 테스트 완료
- [x] 사용자 보완요청 반영 완료
- [x] 사용자 완료 동의 (2026-09-08, 미상신내역 로그인 자동화 범위)

---

## 9단계: 배포 및 최종 운영 검증

### 개발내용

- 실행파일과 필수 설정파일 패키징
- 배포용 AES 암호화 직원 메일정보 생성
- OCR, 브라우저 자동화 및 Outlook 실행환경 검사
- 로그의 개인정보 마스킹과 영수증 임시파일 정리
- 프로그램 업데이트 및 설정 백업·복원 절차 정의
- 운영환경 장애 복구절차 작성

### 사용자 테스트

- 개발환경이 아닌 운영 PC에 신규 설치
- 프로그램 종료와 재실행 후 설정 및 이력 확인
- 로그인정보 미저장 확인
- 암호화 직원정보 파일 직접 열람 불가 확인
- 미상신건수와 미처리건수 전체 시나리오 최종 시험
- Outlook 실제 운영정책에 따른 최종 시험

### 완료 기준

- 운영 PC에서 전체 업무가 재현된다.
- 평문 직원정보와 로그인정보가 배포 폴더에 없다.
- 치명적인 오류 없이 사용자 승인 시나리오를 완료한다.
- 최종 사용자가 운영 버전 완료에 동의한다.

### 최종 승인

- [ ] 설치 검증 완료
- [ ] 보안 검증 완료
- [ ] 미상신건수 운영 검증 완료
- [ ] 미처리건수 운영 검증 완료
- [ ] 사용자 최종 완료 동의

## 5. 사용자에게 추가로 받아야 할 자료

- 계정명별 최초 판정기준과 정상·이상 사례
- 영수증 뷰어 화면과 다양한 실제 영수증 표본
- 전표처리 버튼 순서와 성공·실패 화면
- 팀 전표담당자 정보
- 미상신 이상 건 메일 제목·본문·첨부열
- 미처리 사용자 알림 메일 제목·본문·첨부열·재발송 정책
- 로그인 화면, 팝업 및 e-ACC 아이콘 캡처

## 6. 변경 이력

| 버전 | 일자 | 변경내용 | 사용자 확인 |
|---|---|---|---|
| 0.1 | 2026-08-28 | 최초 개발 기준 및 9단계 승인 게이트 작성 | 대기 |
| 0.2 | 2026-08-28 | 1단계 구현, 실제 Edge 다운로드 절차 및 개발자 자체검증 결과 반영 | 사용자 테스트 대기 |
| 0.3 | 2026-08-28 | 미상신내역 불러오기 버튼을 Edge 자동수집 방식으로 변경 | 개발 중 |
| 0.4 | 2026-08-31 | 로그인 세션 연결, 메뉴 이동·검색·다운로드·137건 표시 통합검증 완료 | 사용자 테스트 중 |
| 0.5 | 2026-08-31 | 1단계 사용자 완료 동의, 2단계 영수증 원본 다운로드 개발 시작 | 1단계 완료 |
| 0.6 | 2026-08-31 | 2단계 OCR 보완: 분할·기호 오인식 승인번호, 한글/깨진 날짜, 공급가액+부가세 합계 비교 및 `#` 영수증 미첨부 사유의 행 표시 추가 | 2단계 사용자 재시험 대기 |
| 0.7 | 2026-08-31 | 증빙유무 동기화 상태 반영: `#` OCR 검증, `Y` 동기화 대기, `N` 영수증 미등록 분리 및 `#` 전체 순차 OCR 검증 기능 추가 | 2단계 전체 OCR 시험 대기 |
| 0.8 | 2026-08-31 | 사용자 지정 이상 행의 `#` 14건 원본 재다운로드 완료. 대비·확대·상하단 다중 임계값 OCR 및 안전한 최대 해상도 제한 추가 | 2단계 재판독 사용자 확인 대기 |
| 0.9 | 2026-08-31 | 로그인·목록 조회 Edge 창은 기존 방식으로 유지하고, 영수증 결재선 팝업만 생성 직후 최소화하도록 보완 | 2단계 재판독 사용자 확인 대기 |
| 1.0 | 2026-08-31 | 영수증 결재선 팝업을 생성 직후 Windows 수준에서 숨김 처리하도록 보완. 일반 Edge 창은 표시 유지 | 2단계 재판독 사용자 확인 대기 |

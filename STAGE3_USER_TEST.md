# 3단계 행 단위 처리 사용자 테스트 안내

e-Acc가 결재 요청과 행 삭제의 기준 화면입니다. 프로그램은 여러 건을 일괄 결재하지 않고, 한 건의 검증 결과와 처리 이력만 보조합니다.

## 시험 순서

1. 프로그램을 실행하고 `e-Acc 목록 불러오기`를 눌러 최신 미상신내역을 표시한다.
2. 프로그램 표에서 처리할 행 **한 건만** 선택한다.
3. `선택 행 영수증 검증`을 눌러 승인번호·증빙일자·사용금액 판정과 사유를 확인한다.
4. 업종에 `PG일반`이 포함된 행이면 `선택 PG 상호 조회`를 누른다.
   - 사업자번호와 비즈노상호가 조회됐는지 확인한다.
   - e-Acc의 **같은 행**에서 `선택` 돋보기를 더블클릭해 실구매처 등록 팝업을 연다.
   - 프로그램에서 `열린 실구매처 팝업 자동등록`을 누르고 확인한다. 프로그램이 사업자번호·사업자명을 자동 입력하고 `등록`까지 클릭한다.
   - 팝업은 프로그램이 실행한 자동화용 Edge에서 열어야 한다. 사용자는 값을 직접 입력하지 않는다.
5. e-Acc에서 같은 행 **한 건만** 결재 요청한다.
6. 프로그램에서 같은 행을 선택한 채 `결재 후 목록 재조회`를 누른다.
7. 최신 목록에서 해당 행이 사라졌다면 프로그램 표의 `처리결과`와 `처리 결과 이력` 탭에 `처리 완료`가 기록되는지 확인한다.

## 기대 결과

- e-Acc 결재 요청 전에는 프로그램이 어떤 행도 자동 결재하거나 삭제하지 않는다.
- 결재 요청 뒤 행이 목록에 남아 있으면 `목록 유지`로 표시한다.
- 행이 사라졌을 때만 `처리 완료`로 표시한다.
- 최신 목록으로 다시 판단하므로 이전 행 번호나 화면 위치를 다음 거래에 사용하지 않는다.
- `처리 결과 이력`에는 영수증 판정, PG 상호조회, 실구매처 자동등록, 결재 결과가 시간순으로 남는다.

## 완료 동의

위 시나리오가 정상이라면 채팅에 `3단계 행 단위 처리 완료 동의`라고 알려 주세요. 문제가 있으면 e-Acc 화면과 프로그램의 `처리 결과 이력` 화면을 함께 캡처해 주세요.
